[build]
builder = "DOCKERFILE"

[deploy]
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 10
startCommand = "/bin/sh -c 'uvicorn coinex_trade_bot.web_app:app --host 0.0.0.0 --port ${PORT:-8000}'"
