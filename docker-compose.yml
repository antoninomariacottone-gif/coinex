services:
  coinex-trade-bot:
    build: .
    container_name: coinex-trade-bot
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./runtime:/app/runtime
